{
  "name": "protected-paths",
  "target": "push",
  "enforcement": "active",
  "_comment": "Repo-level push ruleset: takes no conditions (repository_name is an org-level selector and is rejected here). Requires an org-owned repo; on a personal repo GitHub rejects push rules outright and CODEOWNERS is the weaker review-time fallback.",
  "bypass_actors": [
    {
      "actor_id": "${ARCHILLES_ARCHITECT_APP_ID}",
      "actor_type": "Integration",
      "bypass_mode": "always"
    },
    {
      "actor_id": 5,
      "actor_type": "RepositoryRole",
      "bypass_mode": "always"
    }
  ],
  "rules": [
    {
      "type": "file_path_restriction",
      "parameters": {
        "restricted_file_paths": [
          "archilles/**",
          "schema/**",
          ".github/**"
        ]
      }
    }
  ]
}
